receivers:
  otlp:
    protocols:
      http:

processors:
 batch:

exporters:
 otlp:
   endpoint: http://tempo:3302

service:
 pipelines:
   traces:
     receivers: [otlp]
     processors: [batch]
     exporters: [otlp]